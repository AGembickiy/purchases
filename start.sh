#!/bin/bash

# Универсальный скрипт запуска проекта Purchases
# Автоматически определяет и запускает все необходимые компоненты

echo "🚀 Запуск проекта Purchases..."

# Переходим в директорию проекта
cd "$(dirname "$0")"

# Активируем виртуальное окружение если оно существует
if [ -d "venv" ]; then
    echo "🔄 Активация виртуального окружения..."
    source venv/bin/activate
elif [ -d ".venv" ]; then
    echo "🔄 Активация виртуального окружения..."
    source .venv/bin/activate
fi

# Запускаем основной Python скрипт
python3 start.py "$@"
